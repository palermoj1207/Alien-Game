import pygame


class Link:
    """A class to manage Link."""

    def __init__(self, ai_game):
        """Initialize Link and set his starting position."""
        self.screen = ai_game.screen
        self.settings = ai_game.settings
        self.screen_rect = ai_game.screen.get_rect()

        # Load the Link image and get its rect.
        self.image = pygame.image.load("images/link.bmp")
        self.rect = self.image.get_rect()

        # Start each new Link at the bottom center of the screen.
        self.rect.center = self.screen_rect.center

        # Store a float for links exact horizontal position
        self.x = float(self.rect.x)
        self.y = float(self.rect.y)

        # Movement flag; start with a ship thats not moving
        self.moving_up = False
        self.moving_down = False
        self.moving_left = False
        self.moving_right = False
    
    def update(self):
        """"Update the ships position based on the movement flag"""
        # if self.moving_left:
        #     self.rect.x -= 1
        # elif self.moving_right:
        #     self.rect.x += 1
        # elif self.moving_up:
        #     self.rect.y -= 1
        # elif self.moving_down:
        #     self.rect.y += 1

        # Update link's x value, not the rectangle
        if self.moving_left:
            self.x -= self.settings.link_speed
        elif self.moving_right:
            self.x += self.settings.link_speed
        elif self.moving_up:
            self.y -= self.settings.link_speed
        elif self.moving_down:
            self.y += self.settings.link_speed
        
        # Update rect object from self.x and self.y
        self.rect.x = self.x
        self.rect.y = self.y

    

    def blitme(self):
        """Draw Link at his current location."""
        self.screen.blit(self.image, self.rect)






